server:
  port: 8189
  servlet:
    context-path: "/store"
spring:
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: false
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:mydatabase
    username: sa
    password:
  jpa:
    show-sql: true